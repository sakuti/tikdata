{
  "name": "tikdata",
  "version": "1.0.0",
  "description": "Data archiving tool that extracts TikTok user data into JSON for offline storage and analysis.",
  "license": "MIT",
  "author": "Saku (saq.fi)",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "node --env-file=.env ./scripts/test.js",
    "export": "node --env-file=.env ./scripts/export.js",
    "generate-statistics": "node ./scripts/generate-statistics.js",
    "web": "cd interface && npm run dev"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "express": "^5.2.1",
    "ora": "^9.1.0"
  }
}
